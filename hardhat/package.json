{
  "name": "agritrace-hardhat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network hardhat"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.0.0",
    "ethers": "^5.7.0",
    "hardhat": "^2.17.0"
  }
}
